ALGORITHM algorithm_name
VAR
T :ARRAY_OF INTEGER [];
  min : INTEGER := T[1];
    
BEGIN
  FOR i FROM 1 TO T . length STEP 1 Do
    IF (T [1] < min) THEN
        min : = T [i];
    END_IF  
  END_FOR
  
  Write("le minimum est :",min)
 END